from setuptools import setup, find_packages


setup(
    name='rhizome',
    version='0.0.1',
    author='Scott Barnes',
    author_email='sgbarnes@protonmail.com',
    description='The Rhizome protocol provides distributed and decentralized code execution that is robust to system '
                'and network failures.',
    packages=find_packages(),
    install_requires=[],
)
